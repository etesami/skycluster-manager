---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: skydeployments.core.skycluster.savitestbed.ca
spec:
  group: core.skycluster.savitestbed.ca
  names:
    kind: SkyDeployment
    listKind: SkyDeploymentList
    plural: skydeployments
    shortNames:
    - skydeploy
    singular: skydeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SkyDeployment is the Schema for the skydeployments API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SkyDeploymentSpec defines the desired state of SkyDeployment
            properties:
              appName:
                description: AppName refers to the name of the sky application
                type: string
              deploymentRef:
                description: DeploymentRef is a reference to a Deployment resource
                  in the same namespace
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - appName
            - deploymentRef
            type: object
          status:
            description: SkyDeploymentStatus defines the observed state of SkyDeployment
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
